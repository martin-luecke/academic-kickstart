import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Uncomment the following for static export (GitHub Pages):
  // output: "export",
  // trailingSlash: true,
  // images: { unoptimized: true },

  images: {
    remotePatterns: [],
  },
};

export default nextConfig;
